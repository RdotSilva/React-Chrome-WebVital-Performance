import React, { useState } from "react";

export default function History() {
  const [urlHistory, setUrlHistory] = useState({});

  return (
    <div>
      <h1>Historic Data</h1>
    </div>
    //   TODO: Add historical data for pages. Going to need to cache this data
  );
}
